export default function(/* server */) {
  /*
    Seeds the database with default data.
    In this case, we don't need to seed any data since we're just
    handling authentication with hardcoded values.
  */
}
