<div class="h-screen flex flex-col">
  {{!-- Header --}}
  <header class="h-14 bg-purple-800 flex items-center px-4 border-b border-purple-900">
    <div class="flex-1 flex items-center max-w-3xl">
      <input type="text" placeholder="Search..." class="w-full px-4 py-1.5 rounded bg-purple-700 text-white placeholder-purple-300 focus:outline-none focus:ring-2 focus:ring-purple-500">
      <button class="ml-2 px-4 py-1.5 bg-purple-600 rounded hover:bg-purple-500 text-white">Search</button>
    </div>
  </header>

  {{!-- Main Content --}}
  <div class="flex-1 flex overflow-hidden">
    {{!-- Column 1: Workspace Strip --}}
    <div class="w-16 bg-purple-900 flex flex-col items-center py-4 relative">
      <div class="w-10 h-10 bg-purple-600 rounded-lg flex items-center justify-center text-white font-bold">
        WS
      </div>
      <button 
        class="mt-auto w-8 h-8 bg-gray-300 rounded-full flex items-center justify-center hover:opacity-90"
        {{on "click" (fn (mut this.isProfileOpen) (not this.isProfileOpen))}}
        {{!-- {{on "mousedown" (fn (mut this.isProfileOpen) (not this.isProfileOpen))}} --}}
      >
        DP
      </button>

      <UserProfilePopup 
        @isOpen={{this.isProfileOpen}} 
        @onClose={{fn (mut this.isProfileOpen) false}}
      />
    </div>

    {{!-- Column 2: Channels & DMs --}}
    <div class="w-60 bg-purple-800 text-white p-4 overflow-y-auto">
      {{!-- Channels Section --}}
      <div class="mb-6">
        <h2 class="text-sm font-semibold text-purple-200 mb-2">Channels</h2>
        {{#if @model.channels}}
          <div class="space-y-1">
            {{#each @model.channels as |channel|}}
              <div 
                class="px-2 py-1 rounded cursor-pointer {{if (eq channel.id this.selectedChannelId) 'bg-purple-700' 'hover:bg-purple-700'}}"
                {{on "click" (fn this.selectChannel channel.id)}}
              >
                # {{channel.name}}
              </div>
            {{/each}}
          </div>
        {{else}}
          <div class="px-2 py-1 text-purple-300">
            <div class="animate-pulse flex space-x-2 items-center">
              <div class="h-2 bg-purple-600 rounded w-24"></div>
            </div>
          </div>
        {{/if}}
      </div>

      {{!-- Direct Messages Section --}}
      <div>
        <h2 class="text-sm font-semibold text-purple-200 mb-2">Direct Messages</h2>
        {{#if @model.directMessages}}
          <div class="space-y-1">
            {{#each @model.directMessages as |user|}}
              <div 
                class="px-2 py-1 rounded cursor-pointer {{if (eq user.id this.selectedUserId) 'bg-purple-700' 'hover:bg-purple-700'}} flex items-center"
                {{on "click" (fn this.selectUser user.id)}}
              >
                <div class="w-4 h-4 bg-green-500 rounded-full mr-2"></div>
                {{user.name}}
              </div>
            {{/each}}
          </div>
        {{else}}
          <div class="px-2 py-1 text-purple-300">
            <div class="animate-pulse flex space-x-2 items-center">
              <div class="w-4 h-4 bg-purple-600 rounded-full"></div>
              <div class="h-2 bg-purple-600 rounded w-24"></div>
            </div>
          </div>
        {{/if}}
      </div>
    </div>

    {{!-- Messages Column --}}
    <div class="flex-1 bg-white overflow-y-auto p-6">
      <div class="space-y-6">
        {{#if this.messages}}
          {{#each this.messages as |message|}}
            <div class="flex space-x-4">
              <div class="w-10 h-10 bg-purple-200 rounded-full flex-shrink-0 flex items-center justify-center">
                {{message.user.avatar}}
              </div>
              <div class="flex-1">
                <div class="flex items-baseline">
                  <span class="font-bold">{{message.user.name}}</span>
                  <span class="ml-2 text-sm text-gray-400">{{message.timestamp}}</span>
                </div>
                <p class="mt-1">{{message.content}}</p>
                <div class="mt-2 flex items-center space-x-4">
                  <button class="text-gray-400 hover:text-red-500">❤️</button>
                  <button 
                    class="text-sm {{if (eq this.selectedMessage.id message.id) 'text-purple-800 font-medium' 'text-purple-600'}} hover:underline"
                    {{on "click" (fn this.showThread message)}}
                  >
                    {{message.replyCount}} replies
                  </button>
                </div>
              </div>
            </div>
          {{/each}}
        {{else}}
          <div class="space-y-4">
            {{#each (array 1 2 3) as |placeholder|}}
              <div class="animate-pulse flex space-x-4">
                <div class="w-10 h-10 bg-gray-200 rounded-full"></div>
                <div class="flex-1 space-y-2">
                  <div class="h-2 bg-gray-200 rounded w-1/4"></div>
                  <div class="h-2 bg-gray-200 rounded w-3/4"></div>
                </div>
              </div>
            {{/each}}
          </div>
        {{/if}}
      </div>
    </div>

    {{!-- Thread Column --}}
    {{#if this.isThreadVisible}}
      <div class="hidden md:block w-96 border-l-4 border-gray-200 bg-gray-50">
        <div class="h-full flex flex-col">
          <div class="px-4 py-5 border-b border-gray-200 flex justify-between items-center">
            <h3 class="text-lg font-bold">Thread</h3>
            <button 
              class="text-gray-400 hover:text-gray-600"
              {{on "click" this.closeThread}}
            >
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
          </div>
          
          {{#if this.selectedMessage}}
            <div class="flex-1 overflow-y-auto">
              {{!-- Original message with consistent padding --}}
              <div class="p-5">
                <div class="flex space-x-4">
                  <div class="w-10 h-10 bg-purple-200 rounded-full flex-shrink-0 flex items-center justify-center">
                    {{this.selectedMessage.user.avatar}}
                  </div>
                  <div class="flex-1">
                    <div class="flex items-baseline">
                      <span class="font-bold">{{this.selectedMessage.user.name}}</span>
                      <span class="ml-2 text-sm text-gray-400">{{this.selectedMessage.timestamp}}</span>
                    </div>
                    <p class="mt-1">{{this.selectedMessage.content}}</p>
                  </div>
                </div>
              </div>
              
              <div class="border-b-4 border-gray-200"></div>
              
              <div class="p-4">
                {{#if this.isLoadingReplies}}
                  <div class="space-y-4">
                    {{#each (array 1 2 3) as |placeholder|}}
                      <div class="animate-pulse flex space-x-4">
                        <div class="w-8 h-8 bg-gray-200 rounded-full"></div>
                        <div class="flex-1 space-y-2">
                          <div class="h-2 bg-gray-200 rounded w-1/4"></div>
                          <div class="h-2 bg-gray-200 rounded w-1/2"></div>
                        </div>
                      </div>
                    {{/each}}
                  </div>
                {{else}}
                  <div class="space-y-4">
                    {{#each this.replies as |reply|}}
                      <div class="flex space-x-4">
                        <div class="w-8 h-8 bg-purple-200 rounded-full flex-shrink-0 flex items-center justify-center text-sm">
                          {{reply.user.avatar}}
                        </div>
                        <div class="flex-1">
                          <div class="flex items-baseline">
                            <span class="font-bold text-sm">{{reply.user.name}}</span>
                            <span class="ml-2 text-xs text-gray-400">{{reply.timestamp}}</span>
                          </div>
                          <p class="mt-1 text-sm">{{reply.content}}</p>
                          {{#if reply.reactionCount}}
                            <div class="mt-2">
                              <span class="text-xs text-gray-400">❤️ {{reply.reactionCount}}</span>
                            </div>
                          {{/if}}
                        </div>
                      </div>
                    {{/each}}
                  </div>
                {{/if}}
              </div>
            </div>
          {{/if}}
        </div>
      </div>
    {{/if}}
  </div>
</div>